#include "GridRegion.h"

namespace rwe
{
    GridRegion::GridRegion(unsigned int x, unsigned int y, unsigned int width, unsigned int height)
        : x(x), y(y), width(width), height(height)
    {
    }
}
